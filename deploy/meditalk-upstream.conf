# TAXI-TALK upstream.
# conf.d 파일은 http 컨텍스트에 include 되므로 upstream 만 단독으로 둘 수 있다
# (기존 interpreter-upstream.conf / tts-upstream.conf 와 같은 방식).
upstream taxitalk_web {
    server 127.0.0.1:18092;
    keepalive 16;
}
